import React, {FC} from 'react';
import Layout from '../../layouts/Layout';
import Section from '../../components/Section';

const Create: FC<object> = () => {
  return (
    <Layout title="Create Employee">
      <Section>
        Test
      </Section>
    </Layout>
  )
};

export default Create;
